apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-disk
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard-rwo
